[server]
headless = true
enableCORS = false
enableXsrfProtection = false
# Render / 云平台会通过环境变量 PORT 注入端口; startCommand 里已传 --server.port=$PORT

[browser]
gatherUsageStats = false

[theme]
base = "light"

pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
jieba>=0.42
joblib>=1.3
openpyxl>=3.1
xlsxwriter>=3.2
streamlit>=1.30
regex>=2024.4

# 加强模型可选依赖 (用 --variant enhanced 时需要):
#   - sentence-transformers
#   - torch
# Streamlit Cloud 部署默认 *不* 安装上面两个 (体积大). 在 app 内点
# "📦 安装加强模型依赖" 按钮即可按需安装. 本地用户如想直接用 enhanced,
# 取消下面两行的注释:
# sentence-transformers>=2.2
# torch>=2.0

